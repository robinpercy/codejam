#!/usr/bin/python

import sys
import math

:

def solve(K, N):
    keyTypes = sys.stdin.readline().split()
    chests = []
    for n in range(N):
        chests.append

T = sys.stdin.readline()
for t in range(1, T+1):
    [K, N] = sys.stdin.readline().split()
    solve(int(K), int(N))

